# 라이브러리 인벤토리 관리 및 문서화 계획 (2026-03-16)

본 문서는 `Z:\2_DrivingPlate_Library_nclc` 마스터 소스를 체계적인 라이브러리로 구축하기 위한 엑셀 기반 문서화 전략을 담고 있습니다.

---

## 📋 라이브러리 문서 포함 항목 (필수 컬럼)
기존 관리 문서를 기반으로 실무에 필요한 핵심 항목을 다음과 같이 구성합니다.

1.  **소스 명칭**: (예: 251104_D21_영동대교 남향)
2.  **영상 길이**: (분할 편집본 기준 또는 원본 전체 길이)
3.  **리플렉션 타입**: 
    - `360_Spherical`: 전체 360도 촬영본
    - `Top_Only`: 천장 각도(Top)만 촬영된 형태
    - `N/A`: 리플렉션 소스 없음
4.  **특이사항**: 터널 진출입, 날씨 변화, 신호대기 여부 등
5.  **사용 프로젝트**: 해당 소스가 실제 활용된 상업 프로젝트 명칭
6.  **시간대**: `Day`, `Night`, `Magic Hour`, `Dawn` 등 (파일명 기반 추출)
7.  **유튜브 업로드**: 업로드 완료 여부 및 링크 상태

---

## 📂 작업 프로세스 (안전 모드)
시스템 부하 및 메모리 부족 현상을 방지하기 위해 단계를 나누어 수행합니다.

### 1단계: 소스 데이터 추출 (Investigation)
- 마스터(Z:)의 모든 폴더명과 내부 파일 정보를 스캔하여 기본 리스트를 확보합니다.
- 파일명에서 날짜, ID, 시간대 코드를 자동으로 분리하여 기초 데이터를 만듭니다.

### 2단계: 메타데이터 매칭 (Data Merging)
- 기존 엑셀(`ENMVP...xlsx`)에 있던 프로젝트 이력 및 특이사항 데이터를 신규 리스트와 병합합니다.
- 분할 편집 중인 소스(10s_edit 등)에 대한 정보를 별도로 마킹합니다.

### 3단계: 라이브러리 문서 생성 (Artifact)
- 최종적으로 `02_전체_라이브러리_인벤토리.md` (마크다운 버전)와 엑셀로 내보내기 가능한 `CSV` 형태의 문서를 생성합니다.

---

## 🛠️ 실무 운영 규칙
1.  **비선호 각도 제외**: 앞선 작업에서 분리한 `01, 05, 07, 11`번 각도는 라이브러리 메인 리스트에서 제외하여 가독성을 높입니다.
2.  **버전 관리**: 신규 소스 입고 시 Antigravity AI가 자동으로 리스트를 업데이트하고, 변경 이력을 `03_데이터_관리_및_업데이트_절차.md`에 기록합니다.

---

## ✅ 다음 단계
1.  기존 엑셀 파일(`ENMVP 드라이빙플레이트 라이브러리_260202.xlsx`)의 내용을 분석하여 현재 보유 중인 텍스트 데이터를 추출하겠습니다.
2.  추출된 데이터와 현재 Z: 드라이브의 실제 파일 상태를 대조하여 업데이트하겠습니다.

---
*작성자: Antigravity AI*
